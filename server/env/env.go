package script

// func (s *script.Server) GetEnv() []string {
// 	env := make([]string, 0, len(s.Env))
// 	for k, v := range s.Env {
// 		env = append(env, k+"="+v)
// 	}
// 	golog.Info(env)
// 	return env
// }
